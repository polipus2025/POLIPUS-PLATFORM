# 🛰️ GOOGLE EARTH ENGINE SPATIAL INTELLIGENCE CONFIGURATION
# For enhanced EUDR deforestation monitoring (additive layer)

# Google Earth Engine Project ID
GEE_PROJECT_ID=agritrace-spatial-intelligence

# Google Cloud Service Account Credentials (JSON format)
# This should be the complete JSON service account key from Google Cloud Console
# Example: {"type": "service_account", "project_id": "your-project", ...}
GOOGLE_APPLICATION_CREDENTIALS={}

# Optional: Custom GEE API endpoint (leave blank for default)
GEE_API_ENDPOINT=https://earthengine.googleapis.com/v1

# GEE Analysis Settings
GEE_HANSEN_DATASET=UMD/hansen/global_forest_change_2023_v1_11
GEE_SENTINEL_COLLECTION=COPERNICUS/S2_SR_HARMONIZED
GEE_DEFAULT_TREE_COVER_THRESHOLD=30

# Enhanced EUDR Configuration
EUDR_CUTOFF_DATE=2020-12-31
GEE_CONFIDENCE_THRESHOLD=70
GEE_RISK_SCORE_THRESHOLD=25

# Performance Settings
GEE_BATCH_SIZE=10
GEE_TIMEOUT_SECONDS=30
GEE_RETRY_ATTEMPTS=3